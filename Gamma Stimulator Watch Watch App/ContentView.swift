import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 10) {
            Text("Sleep Monitor")
                .font(.headline)
            Text("Status: Active")
                .font(.footnote)
        }
    }
}
